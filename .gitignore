.vscode/
__pycache__/
*.pyc

wandb/
results/
bsub_outputs/
.hyperparameter_search_scripts_main/
# code graph for the research-buddy brain app (rebuild: graphify update . --no-cluster && graphify cluster-only . --no-label)
graphify-out/
# local run-ledger staging
experiment-runs/
# HF datasets .map() cache files written next to load_from_disk dirs
**/cache-*.arrow
# training outputs (run records live in experiments/runs/, tracked)
checkpoints/
# throwaway worktrees created by experiments/runs/*/launch.sh re-runs
.rerun-worktrees/
# nohup / launcher logs
logs/
env.sh